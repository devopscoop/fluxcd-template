# https://fluxcd.io/flux/components/helm/helmreleases/
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: gathio
spec:
  interval: 10m
  valuesFrom:
    - kind: ConfigMap
      name: gathio-values
    - kind: Secret
      name: gathio-secrets
  chartRef:
    kind: OCIRepository
    name: devopscoop-app
