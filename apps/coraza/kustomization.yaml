apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Envoy Gateway policies must live in the same namespace as the Gateway they
# target, so this deploys into envoy-gateway-system (owned by apps/eg).
namespace: envoy-gateway-system

resources:
  - envoyextensionpolicy.yaml
